name: brew test-bot
on:
  push:
  pull_request:
jobs:
  test-bot:
    runs-on: ubuntu-latest
    container: ghcr.io/bo98/brew-ubuntu16.04:brew-deprecate
    steps:
      - run: brew test-bot --only-cleanup-before

      - run: brew test-bot --only-setup
